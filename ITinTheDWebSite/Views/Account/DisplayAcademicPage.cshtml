@model ITinTheDWebSite.Models.AcademicModel
@{
    ViewBag.Title = "StoreAcademicPage";
}
<!-- Modal -->
<!-- Button to trigger modal -->
<a href="#AcademicModel" style="color: Black" role="button" class="btn" data-toggle="modal">
    Share your experiences</a>
<div id="AcademicModel" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="academicExp"
    aria-hidden="true" style="width: 400px; height: 450px;">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
            ×</button>
        <h3 id="myAcademicLabel">
            Academic Institution Information</h3>
    </div>
    @using (Html.BeginForm("StoreAcademicPage", "Account"))
    {
        @Html.ValidationSummary(true)
        <div class="well">
            <fieldset>
                <h6>
                    * Empty the text area to delete your information.</h6>
                <div class="editor-label">
                    Share your information:
                </div>
                <div class="editor-field">
                    @Html.TextAreaFor(model => model.AcademicInstitutionTextField)
                    @Html.ValidationMessageFor(model => model.AcademicInstitutionTextField)
                </div>
                <p>
                    <input type="submit" value="Submit" />
                </p>
            </fieldset>
        </div>
    }
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
