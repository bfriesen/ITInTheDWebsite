@model ITinTheDWebSite.Models.ProspectModel
@{
    ViewBag.Title = "DisplayProspectPage";
}
<!-- Modal -->
<!-- Button to trigger modal -->
<a href="#ProspectModel" style="color: Black" role="button" class="btn" data-toggle="modal">
    Share your experiences</a>
<div id="ProspectModel" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="prospectExp"
    aria-hidden="true" style="width: 400px; height: 450px;">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
            ×</button>
        <h3 id="myProspectLabel">
            Graduate student experience</h3>
    </div>
    @using (Html.BeginForm("StoreProspectPage", "Account"))
    {
        @Html.ValidationSummary(true)
        <div class="well">
            <fieldset>
                <h6>
                    * Empty the text area to delete your information.</h6>
                <div class="editor-label">
                    Share your experience:
                </div>
                <div class="editor-field">
                    @Html.TextAreaFor(model => model.ProspectiveStudentTextField)
                    @Html.ValidationMessageFor(model => model.ProspectiveStudentTextField)
                </div>
                <p>
                    <input type="submit" value="Submit" />
                </p>
            </fieldset>
        </div>
    }
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
